import 'package:flutter/material.dart';

class SchoolDay13 extends StatelessWidget {
  const SchoolDay13({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Ini Halaman School"),
        backgroundColor: Colors.cyan,
      ),
    );
  }
}
