import 'package:flutter/material.dart';

class BussinesDay13 extends StatelessWidget {
  const BussinesDay13({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Ini Halaman Bussines"),
        backgroundColor: Colors.cyan,
      ),
    );
  }
}
